/* UI: 设置视图 */
#ifndef PCRETRO_UI_SETTINGS_VIEW_H
#define PCRETRO_UI_SETTINGS_VIEW_H

#include <windows.h>

HWND pcretro_settings_view_create(HWND parent);
void pcretro_settings_view_refresh(HWND self);

#endif
